@model IEnumerable<DaNangTourism.Models.ProcessingReport>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.prcContent)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.processTime)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.prcContent)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.processTime)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.prcReportID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.prcReportID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.prcReportID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
